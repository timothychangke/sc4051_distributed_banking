% =============================================================================
%  SC4051 Distributed Systems — Lab Report
%  Design and Implementation of a Distributed Banking System
% =============================================================================
\documentclass[9pt,a4paper]{article}

% ── Page geometry ────────────────────────────────────────────────────────────
\usepackage[
  top=2.0cm, bottom=2.0cm, left=2.2cm, right=2.2cm
]{geometry}

% ── Core packages ────────────────────────────────────────────────────────────
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
% Font packages removed (not available in this TeX distribution)
\usepackage{amsmath}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{float}
\usepackage{enumitem}
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{caption}
\usepackage{subcaption}
\usepackage[table]{xcolor}
\usepackage{listings}

% ── Byte-level wire diagrams ────────────────────────────────────────────────
\usepackage{bytefield}

% ── TikZ for architecture diagrams ──────────────────────────────────────────
\usepackage{tikz}
\usetikzlibrary{
  arrows.meta,
  positioning,
  shapes.geometric,
  fit,
  calc,
  backgrounds,
  decorations.pathreplacing
}

% ── Code listings ────────────────────────────────────────────────────────────
\usepackage{listings}
\definecolor{codebg}{HTML}{F5F5F5}
\definecolor{codeframe}{HTML}{CCCCCC}
\definecolor{codecomment}{HTML}{6A737D}
\definecolor{codekeyword}{HTML}{D73A49}
\definecolor{codestring}{HTML}{032F62}
\lstset{
  basicstyle=\ttfamily\footnotesize,
  backgroundcolor=\color{codebg},
  frame=single,
  rulecolor=\color{codeframe},
  commentstyle=\color{codecomment}\itshape,
  keywordstyle=\color{codekeyword}\bfseries,
  stringstyle=\color{codestring},
  breaklines=true,
  showstringspaces=false,
  tabsize=4,
  xleftmargin=1em,
  framexleftmargin=0.5em,
  aboveskip=0.8em,
  belowskip=0.5em,
}

% ── Headers and page numbering ───────────────────────────────────────────────
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small SC4051 Distributed Systems}
\fancyhead[R]{\small Lab Report}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}

% ── Hyperlinks (load last) ──────────────────────────────────────────────────
\usepackage[
  colorlinks=true,
  linkcolor=blue!60!black,
  citecolor=blue!60!black,
  urlcolor=blue!60!black
]{hyperref}

% ── Custom colours for diagrams ──────────────────────────────────────────────
\definecolor{headerblue}{HTML}{3B82F6}
\definecolor{tlvgreen}{HTML}{10B981}
\definecolor{replypurple}{HTML}{8B5CF6}
\definecolor{callbackorange}{HTML}{F59E0B}
\definecolor{lightbluebg}{HTML}{EFF6FF}
\definecolor{lightgreenbg}{HTML}{ECFDF5}
\definecolor{lightpurplebg}{HTML}{F5F3FF}
\definecolor{lightorangebg}{HTML}{FFFBEB}
\definecolor{serverblue}{HTML}{2563EB}
\definecolor{clientred}{HTML}{DC2626}

% ── Compact section spacing ──────────────────────────────────────────────────
\usepackage{titlesec}
\titlespacing*{\section}{0pt}{1.2em}{0.6em}
\titlespacing*{\subsection}{0pt}{0.9em}{0.4em}
\titlespacing*{\subsubsection}{0pt}{0.7em}{0.3em}

% ── Paragraph spacing ───────────────────────────────────────────────────────
\setlength{\parskip}{0.4em}
\setlength{\parindent}{0em}
\captionsetup{skip=6pt}

% =============================================================================
\begin{document}

% ─────────────────────────────────────────────────────────────────────────────
%  COVER PAGE
% ─────────────────────────────────────────────────────────────────────────────
\begin{titlepage}
    \centering
    
    \includegraphics[width=0.6\textwidth]{NTU_Logo-scaled.png}  
    
    \vfill 
    
    \vspace{1.5cm}
    {\Large \textbf{Assignment: Distributed Banking System}} \\
    
    % Commented out Project Code
    % \vspace{0.5cm}
    % CCDS26-\\
    
    \vspace{1cm}
    
    % Author Block
    {\large by\\}
    \vspace{0.5cm}
    \begin{tabular}{l l l}
      \toprule
      \textbf{Name} & \textbf{Matric No.} & \textbf{Work \%} \\
      \midrule
      Timothy Chang  & \textit{U2220136J} & 33.33\% \\
      O Jing         & \textit{UXXXXXXX} & 33.33\% \\
      Neo Zhi Xuan   & \textit{U2222293E} & 33.33\% \\
      \bottomrule
      \end{tabular}
    
    
    % Push the University/Date block to the very bottom
    \vfill 
    {\huge \textbf{SC4051 Distributed Systems }}\\
    \vfill
    
    % University Name (Standard size, bold - removed \Large)
    {\large \textbf{NANYANG TECHNOLOGICAL UNIVERSITY}}\\
    \vspace{0.5cm}
    {\large April 2026}

\end{titlepage}

\pagenumbering{gobble}

\onecolumn
\tableofcontents
\newpage

% ---------------------------------------------------------
\section{Introduction \& System Overview}
% ~1 page
% - Brief problem statement (distributed banking over UDP, no frameworks).
% - High-level architecture diagram: C++ Client <-> UDP <-> Go Server.
% - Emphasize the cross-language decision and why (confronting real-world data representation mismatches: endianness, string encoding, float IEEE 754 agreement). 
% - Technology choices: Go for server (goroutine-friendly, strong net package), C++ for client (low-level byte control, Winsock/POSIX portability).
The project implements a distributed banking application, using a custom client-server architecture that uses UDP-based inter-process communication. The system is cross-language interoperable and consists of a C++ client application and a Go-based server. These components communicate via a custom binary protocol, without Remote Procedure Call (RPC) frameworks. 

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    node distance=1.8cm and 2.5cm,
    >=Latex,
    font=\small,
    box/.style={
        draw,
        rounded corners=6pt,
        thick,
        align=center,
        minimum width=3.4cm,
        minimum height=1.5cm
    },
    clientbox/.style={
        box,
        draw=clientred,
        fill=lightorangebg
    },
    serverbox/.style={
        box,
        draw=serverblue,
        fill=lightbluebg
    },
    proto/.style={
        draw,
        dashed,
        rounded corners=4pt,
        inner sep=6pt,
        fill=white,
        align=center
    }
]
% Main nodes
\node[clientbox] (client) {
    \textbf{C++ Client Application}\\
    Constructs request messages\\
    Waits for responses / retries
};
\node[serverbox, right=4.8cm of client] (server) {
    \textbf{Go Server}\\
    Listens on UDP socket\\
    Processes requests and replies
};
% Protocol label
\node[proto, above=0.9cm of $(client)!0.5!(server)$] (protocol) {
    \textbf{Custom Binary Protocol} \\
    UDP-based inter-process communication\\
    No RPC framework
};
% Arrows
\draw[->, thick] 
    (client.east) -- node[above, align=center] {
        UDP request datagram\\
        \scriptsize(IP, port known)
    } (server.west);
\draw[<-, thick] 
    (client.east) -- node[below, align=center] {
        UDP response datagram\\
        \scriptsize(client may retry if needed)
    } (server.west);
% Optional grouping
\node[
    draw=black!40,
    dashed,
    rounded corners=8pt,
    fit=(client)(server)(protocol),
    inner sep=10pt,
] {};
\end{tikzpicture}
\caption{High-level architecture of the distributed banking system.}
\label{fig:high-level-architecture}
\end{figure}

At a high level, the client and server communicate via UDP datagrams, on known IP addresses and ports. The client constructs request messages and waits for responses, and may retry depending on the protocol. The server listens for requests and processes each one, before returning a response.

The client is written in C++ due to the language's ability to handle low level byte control for packet construction, as well as portable socket handling across Windows and POSIX. Meanwhile, the choice of Go for the server provides an in-depth networking library and built-in synchronisation primitives for concurrency, to allow for simple implementation of UDP sockets and concurrent client handling.

The presence of cross-language components necessitates handling of various constraints. Firstly, the data boundary and marshalling conventions for all data types must be aligned on both components. Next, it is important to standardise encodings so that both components interpret numeric bytes identically. Network byte order (big-endian) is used for all integer fields, whereas IEEE754 double format (64-bit) is used for floating-point balances.

The report analyses the unreliability of UDP, and the trade-offs between using different semantic modes to handle it, via explicit retry and duplicate-detection mechanisms.

% ---------------------------------------------------------
\section{Message Design \& Wire Protocol}
% ~2.5 pages. Mandatory centrepiece.

\subsection{Packet Header Format}
% Byte-level diagram of the 5-byte semantics header [ServiceID(1B)][RequestID(4B BE)] 
% and the 17-byte reply header [MsgType(1B)][ReqID(4B)][IPv4(4B)][Port(2B)][StatusCode(2B)][ContentLen(4B)].
Each message sent between client and server begins with an 18-byte header, followed by a payload of variable length. It encodes information about protocol control, through these fields:

\begin{table}[h]
\centering
\caption{Packet Header Layout}
\label{tab:packet-header}
\begin{tabular}{|l|c|p{0.6\linewidth}|}
\hline
\textbf{Field} & \textbf{Size} & \textbf{Description} \\
\hline
Type & 1 byte & Indicates whether the datagram is a request (0x00), a normal reply (0x01), or a server-initiated callback (0x02). \\
\hline
Semantics Flag & 1 byte & Encodes invocation semantics: 0x00 for at-least-once, 0x01 for at-most-once. \\
\hline
Request ID & 4 bytes & An identifier chosen by the client, used for duplicate detection and request-response matching. \\
\hline
Client IPv4 Address & 4 bytes & The sender’s IPv4 address. Explicitly informs the server about the client's identity. \\
\hline
Client Port & 2 bytes & The UDP port number of the sender. \\
\hline
Status Code & 2 bytes & Indicates success or error conditions in replies; non-zero values represent errors. \\
\hline
Content Length & 4 bytes & Length (in bytes) of the payload that follows. \\
\hline
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\begin{bytefield}[bitwidth=1.05em]{32}
\bitheader{0-31} \\
% Row 1
\bitbox{8}{\scriptsize Type}
\bitbox{8}{\scriptsize Semantics}
\bitbox{16}{} \\
% Row 2
\bitbox{32}{\scriptsize Request ID} \\
% Row 3
\bitbox{32}{\scriptsize Client IPv4 Address} \\
% Row 4
\bitbox{16}{\scriptsize Client Port}
\bitbox{16}{\scriptsize Status Code} \\
% Row 5
\bitbox{32}{\scriptsize Content Length} \\
\end{bytefield}
\caption{Byte-level representation of the packet header.}
\label{fig:header-byte-layout}
\end{figure}

This header is inspired by traditional RPC or network protocol headers. A fixed header size allows the client and server to parse data with a consistent offset. It also allows for robust validation of datagrams that are truncated. Simultaneously, all multi-byte fields are in big-endian format, aligning the client and server's method of interpreting numeric values.

\subsection{Type-Length-Value (TLV) Payload Encoding}
% Byte-level diagram of [FieldID(1B)][Length(4B BE)][Value(NB)]. 
% Show a concrete worked example (e.g., OpenAccount request with name "Alice", password "pass1234", currency SGD, balance 1000.00). 
% Lay out every byte in a hex table.
After the header, each message carries a TLV-encoded payload of application-specific fields. Fields are optional, and only serialised when it is present. Each field has this format:

\begin{table}[h]
\centering
\caption{TLV Payload Layout}
\label{tab:packet-header}
\begin{tabular}{|l|c|p{0.6\linewidth}|}
\hline
\textbf{Field} & \textbf{Size} & \textbf{Description} \\
\hline
Field ID & 1 byte & Identifies the type of the field (e.g., account number, password, amount). \\
\hline
Length & 4 bytes & Specifies the length of the Value field in bytes, encoded in big-endian format. \\
\hline
Value & Variable & Contains the actual data corresponding to the Field ID. The size is determined by the Length field. \\
\hline
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\begin{bytefield}[bitwidth=1.1em]{8}
\bitheader{0-7} \\
\begin{rightwordgroup}{1 byte}
\bitbox{8}{Field ID}
\end{rightwordgroup} \\
\begin{rightwordgroup}{4 bytes}
\bitbox{8}{Length (MSB)}
\bitbox{8}{}
\bitbox{8}{}
\bitbox{8}{Length (LSB)}
\end{rightwordgroup} \\
\begin{rightwordgroup}{Variable}
\bitbox{32}{Value}
\end{rightwordgroup}
\end{bytefield}
\caption{Type-Length-Value (TLV) encoding used in protocol payloads.}
\label{fig:tlv-format}
\end{figure}


For example, the request "OpenAccount (Alice, pass1234, SGD, 1000.00)" attempts to open an account, and might include these TLV fields:

\begin{table}[h]
\centering
\caption{OpenAccount Request Fields}
\label{tab:packet-header}
\begin{tabular}{|l|c|p{0.6\linewidth}|}
\hline
\textbf{Field Name} & \textbf{Length} & \textbf{Description} \\
\hline
Account Holder Name & N bytes & UTF-8 encoded string representing the user's name. The length varies depending on the number of characters. \\
\hline
Password & Up to 8 bytes & Password represented as raw bytes. If shorter than 8 bytes, it is zero-padded to maintain a fixed maximum length. \\
\hline
Currency Type & 1 byte & Encoded as a predefined enumeration value representing supported currencies (e.g., SGD, USD). \\
\hline
Initial Balance & 8 bytes & IEEE-754 double precision floating-point number encoded in big-endian format. \\
\hline
\end{tabular}
\end{table}

The client packs each numeric or string value  without additional padding, and the length specifies the exact number of bytes. For example, if the user “Alice” (5 letters) chooses password “pass1234” (8 chars), currency SGD (id=1), and balance 1000.00, the TLV encoding for that request would look like:

\begin{lstlisting}[caption={TLV Encoding Example}, label={lst:tlv}]
01 00 00 00 05 41 6C 69 63 65   [Name field: ID=1, Len=5, Value="Alice"]
02 00 00 00 08 70 61 73 73 31 32 33 34   [Password field: ID=2, Len=8, Value="pass1234"]
03 00 00 00 01 01                       [Currency field: ID=3, Len=1, Value=0x01 (SGD)]
04 00 00 00 08 40 8F 40 00 00 00 00 00   [Balance field: ID=4, Len=8, Value=1000.00]
\end{lstlisting}

The server, upon receiving this TLV payload, unpacks each field by reading the ID and length, then extracts the number of bytes as the value. It interprets the raw bytes based on the field's type - converting an 8-byte double to Go's float64 in big-endian, or treating password bytes as an ASCII string.

Meanwhile, certain fields are fixed-size. In the Go server, an 8-byte password field is enforced. This will be discussed in detail in Section~\ref{subsec:marshalchallenge}.

Hence, this format handles data cleanly. The use of ID and length allows for robust parsing. While the encoding is handcrafted in code, it creates an effective platform-independent message format.

\subsection{Callback Push Packet}
% Separate wire format: [MsgType(1B)][ServiceID(1B)][AccNo(4B)][NameLen(4B)][Name(NB)][Currency(1B)][Balance(8B)].

Within the lab requirements, the server should "push" updates to all registered clients during a monitoring interval. When an account is opened, closed, deposited, or withdrawn, the server triggers a callback packet to the client. It uses a different packet format:

\begin{table}[h]
\centering
\caption{Callback Packet Layout}
\label{tab:packet-header}
\begin{tabular}{|l|c|p{0.6\linewidth}|}
\hline
\textbf{Field Name} & \textbf{Size} & \textbf{Description} \\
\hline
MsgType & 1 byte & Always set to 0x02 to differentiate callback packets from normal replies. \\
\hline
Service ID & 1 byte & Indicates the operation to trigger (e.g., OpenAccount). \\
\hline
Account Number & 4 bytes & Integer account number (big-endian). \\
\hline
Name Length & 4 bytes & Length of the account holder’s name in bytes. \\
\hline
Name & Variable & UTF-8 encoded string representing the account holder’s name. \\
\hline
Currency Type & 1 byte & The account's currency code as an enumeration. \\
\hline
Balance & 8 bytes & IEEE-754 double precision floating-point value (big-endian) representing the updated balance. \\
\hline
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\begin{bytefield}[bitwidth=1.05em]{32}
\bitheader{0-31} \\
% Row 1
\bitbox{8}{\scriptsize MsgType}
\bitbox{8}{\scriptsize Service}
\bitbox{16}{} \\
% Row 2
\bitbox{32}{\scriptsize Account Number} \\
% Row 3
\bitbox{32}{\scriptsize Name Length} \\
% Row 4
\bitbox{32}{\scriptsize Name (UTF-8, variable length)} \\
% Row 5
\bitbox{8}{\scriptsize Curr}
\bitbox{24}{} \\
% Row 6
\bitbox{32}{\scriptsize Balance (high 32 bits)} \\
% Row 7
\bitbox{32}{\scriptsize Balance (low 32 bits)} \\
\end{bytefield}
\caption{Byte-level representation of the callback payload.}
\label{fig:callback-byte-layout}
\end{figure}

For example, if account 12345 (“Bob”) with currency USD (0x00) now has balance 250.75, the server might send:

\begin{lstlisting}[caption={Callback Packet Example}, label={lst:callback}]02 05               [MsgType=0x02, ServiceID=0x05 (monitor update)]
00 00 30 39        [Account#=12345 (big-endian)]
00 00 00 03 42 6F 62   [NameLen=3, Name="Bob"]
00                [Currency=0x00 (USD)]
40 6F 40 00 00 00 00 00   [Balance=250.75 (IEEE-754)]
\end{lstlisting}

The client’s UDP socket listens for these packets and, upon receipt, displays the updated account information to the user.

The benefits of having a fixed, known structure is that it avoids the overhead that TLV packets face with self-describing fields. TLV packets require 5 additional bytes for each field to store FieldID and Length, making it slower to encode, decode, and process.

\subsection{Cross-Language Marshalling Challenges}
\label{subsec:marshalchallenge}
% How Go's [8]byte password maps to C++'s variable-length string. 
% How you agreed on IEEE 754 big-endian for float64 <-> double.
Implementing the client and server in different languages always leads to challenges in data coordination, due to the differences in how each language defines its data types and mechanisms. The first challenge was designing the password field. The Go server defines passwords as a fixed 8-byte array, whereas the C++ client uses `std::string`. To overcome this, the protocol's TLC password field was fixed to 8 bytes on the wire. The client would ensure that the string is 8 bytes, by truncating it, or padding it with zeros.

The second issue is the different representation of floating-point values in memory across both languages, where C++ uses `double` and Go uses `float64`. While both languages use IEEE-754 64-bit binary representation for these numbers, they could differ in endianness, and lead to incorrect decoding of raw bytes. Hence, for all floating-point transmissions, values are converted into IEEE-754 format encoded in big-endian order before sending, and recipients would reverse the process. This ensures the same byte sequence is interpreted reliably and correctly.


% ---------------------------------------------------------
\section{System Architecture \& Design}
% ~2 pages

\subsection{Server Architecture (Go)}
% Layered diagram showing cmd/main.go -> semantics/dispatcher -> handler -> banking/service -> store/memory. 
% Explain dependency wiring (closure for BuildHandler).
The Go server is implemented with a layered design. 

\begin{figure}[h]
\centering
\resizebox{\linewidth}{!}{
\begin{tikzpicture}[
    box/.style={
        rectangle,
        draw=black,
        rounded corners,
        minimum width=2.6cm,
        minimum height=0.8cm,
        align=center
    },
    arrow/.style={->, thick}
]
\node[box] (main) {Main};
\node[box, right=1.4cm of main] (dispatcher) {Dispatcher};
\node[box, right=1.4cm of dispatcher] (handler) {Handlers};
\node[box, right=1.4cm of handler] (service) {Services};
\node[box, right=1.4cm of service] (store) {Store};
\draw[arrow] (main) -- (dispatcher);
\draw[arrow] (dispatcher) -- (handler);
\draw[arrow] (handler) -- (service);
\draw[arrow] (service) -- (store);
\end{tikzpicture}
}
\caption{Go server architecture (high-level).}
\label{fig:go-server-architecture}
\end{figure}

Firstly, main function sets up a UDP socket and constructs the server components. 

Next, the dispatcher reads incoming UDP packets, unmarshals the payload and header, before routing each request to an appropriate service handler. Its behaviour depends on the chosen invocation semantics: for at-least-once mode, it forwards each request to the handler, while in at-most-once mode, it checks the request against a history table to eliminate duplicate requests.

Thirdly, the handler receives a request from the dispatcher, and chooses a service to trigger. It contains the application logic for services like opening, closing, depositing and withdrawing from an account. It decodes the TLV fields from the payload and performs validation, before invoking the appropriate service function, and encoding a TLV response.

\begin{figure}[h]
\centering
\begin{tikzpicture}[
    box/.style={
        rectangle,
        draw=black,
        rounded corners,
        minimum width=4.5cm,
        minimum height=3cm,
        align=left,
        font=\small
    }
]

\node[box] (account) {
\textbf{Account} \\ \\
\texttt{Mu : Mutex} \\
\texttt{AccountNumber : uint32} \\
\texttt{HolderName : string} \\
\texttt{Password : [8]byte} \\
\texttt{CurrencyType : uint8} \\
\texttt{Balance : float64}
};

\end{tikzpicture}
\caption{Memory store account structure.}
\label{fig:memory-store-account}
\end{figure}

Lastly, the banking service and memory store layers handle the data operations. The service layer provides operations like OpenAccount that directly manipulate the storage. The memory store maintains a map of accounts, and uses synchronisation primitives to ensure that concurrent access to data is safe and does not lead to race conditions. 

Separately, the monitoring feature is implemented within the banking service with a publish-subscribe mechanism. It stores a map of registered clients with their defined expiration times, and handles the goroutines for dispatching account updates using callback packets.

By separating the logic into layers, we isolate concerns cleanly. For instance, semantics logic is handled in the dispatcher, and does not clutter banking service logic. Protocol encoding is handled only once in the dispatcher and handler. At the same time, this allows dependency injection in tests, by allowing developers to mock the UDP socket at the top level.

The architecture is wired by a BuildHandler function that exposes the banking service and monitor manager to all handlers. This ensures that the dispatcher can make calls to the handler without having to know the underlying banking or monitoring details, hence decoupling these components.

\subsection{Client Architecture (C++)}
% Three-layer diagram (Application -> Protocol -> Network). 
% Highlight dependency injection pattern (BankClient takes interfaces). Explain why this matters (mocking, swappable transport).

The C++ client is implemented with a three-layer architecture.

\begin{figure}[h]
\centering
\resizebox{\linewidth}{!}{
\begin{tikzpicture}[
    box/.style={
        rectangle,
        draw=black,
        rounded corners,
        minimum width=3.5cm,
        minimum height=0.9cm,
        align=center
    },
    arrow/.style={->, thick}
]
\node[box] (app) {Application (BankClient/BankIO)};
\node[box, right=1.8cm of app] (protocol) {Protocol (CommandEncoder/MessageSerialiser)};
\node[box, right=1.8cm of protocol] (network) {Network (UDPSocket)};
\draw[arrow] (app) -- (protocol);
\draw[arrow] (protocol) -- (network);
\end{tikzpicture}
}
\caption{C++ client layered architecture.}
\label{fig:cpp-client-architecture}
\end{figure}

When sending a request from the application layer, the client runs an interactive looping Command Line Interface (CLI) that takes a command from the user corresponding to banking operations. It communicates this request to the protocol layer. The protocol layer constructs a request message by populating the 18-byte header with the chosen invocation semantics flag and a fresh or zeroed request ID. It then converts numbers into big-endian format before encoding arguments into a TLV payload for cross-platform consistency. This layer also manages the decoding of replies and callbacks into structured data. Finally, the network layer abstracts UDP communication by providing send and receive operations over sockets.

When the client receives a response, the response takes the reverse path. It is received at the network layer. Then, the protocol layer validates headers and parses the payload, and thereafter the application layer presents the results or callback updates to the user. This design ensures that user logic, message formatting, and network communication are cleanly decoupled and easy to maintain.

At the same time, dependency injection was used to provide the client with access to various services. The client was able to access interfaces like BaseSocket and BaseCommandEncoder during its lifecycle. This allows the system to mock components for unit testing, and reduces coupling between the interface and networking/marshalling internals.


\subsection{Concurrency Model}
% Server uses sync.RWMutex on the memory store (RLock for reads, Lock for writes). 
% Monitor manager runs its own goroutine for subscriber sweep. 
% Client is single-threaded (blocked during monitor interval, per spec).

The system was designed to allow multiple clients to operate concurrently on the server, and requires various synchronisation strategies to align this.

The in-memory account map, where account numbers were mapped to account data, is protected by a global read-write mutex which synchronises account lifecycle operations like CreateAccount and GetAccount. While multiple read queries can be run under a shared read lock, write-based operations utilise an exclusive write lock.

Each account's state is protected separately using mutexes that belongs to the account itself. After accessing the specific account from the store, the account's mutex is acquired before read or mutation operations, and only unlocked when completed. This design allows clients to concurrently operate on different accounts while ensuring correctness on individual accounts. 

Transfer operations involve two accounts and face the risk of a deadlock, if they hold the resource that the other party requires, and wait indefinitely for it to be free. To avoid this, an ordering is imposed on account locks: the account with the smallest account number is locked first, then the other account is locked. This deterministic order ensures that no two simultaneous transfers will lock the same two accounts in different orders.

The monitor callback mechanism involves a single server and multiple clients. The server maintains a map of active subscribers, protected by a mutex. When a service operation updates an account's state, the handler invokes the `NotifyAll()` function. It marshals the update, acquires the map lock, iterates through all active subcribers, and sends the update to each subscriber over UDP, before releasing the lock. 

In contrast, the client is single threaded. The main thread sends a request and waits for a reply or timeout. In monitoring mode, the thread repeatedly checks for callback packets on the UDP socket during the callback interval. User input is disabled during this period, hence there is no need for a separate thread. 

% ---------------------------------------------------------
\section{Banking Services \& Custom Operations}
% ~1.5 pages

\subsection{Core Services (S1–S4)}
% Brief description of each. Focus on error handling strategy and status code mapping table.

\subsection{Monitor/Callback (S4)}
% Explain registration, subscriber list, sweep goroutine, NotifyAll pushes. 
% Sequence diagram: Client A operates -> Server notifies Client B.

\subsection{Op 5: GetBalance (Idempotent)}
% Define idempotency formally. Explain why GetBalance is safe under retransmission.

\subsection{Op 6: Transfer (Non-Idempotent)}
% Explain the two-account mutation. Show why retransmission is dangerous here. Sets up Section 5.


% ---------------------------------------------------------
\section{Fault Tolerance \& Invocation Semantics}
% ~2.5 pages. Second mandatory centrepiece.

\subsection{At-Least-Once Implementation}
% Timeout + retransmission on client side. Dispatcher always executes. Diagram of flow.

\subsection{At-Most-Once Implementation}
% Request IDs, duplicate filtering at server, ReplyHistory cache. 
% Diagram of cached reply retrieval vs re-execution.

\subsection{Loss Simulator Design}
% How LossSimulator works (configurable drop rate, seeded rand.Rand). 
% Two injection points: request-receive loss and reply-send loss.

\subsection{Experiment Design}
% Table of experiments run (e.g., Deposit $100, loss rate 30%). Use concrete numbers.

\subsection{Results \& Discussion}
% Tables/charts showing outcomes. 
% Punchline: at-least-once is fine for idempotent (GetBalance), dangerous for non-idempotent (Transfer). 
% At-most-once handles everything correctly at the cost of server-side memory.


% ---------------------------------------------------------
\section{Testing Strategy}
% ~1 page

\subsection{Unit Tests}
% C++ client unit tests using GoogleTest + mocks (MockSocket, MockEncoder, MockSerializer).

\subsection{Integration Tests}
% Integration\_test.go: end-to-end tests spawning real processes, validating all services/fault tolerance. 
% Mention lockServer pattern for isolation.


% ---------------------------------------------------------
\section{Assumptions, Limitations \& Future Work}
% ~0.5 pages
% - Assumptions: single currency per account, in-memory only, basic auth.
% - Limitations: reply history grows unbounded without eviction, no wire encryption.
% - Future work: persistent storage, TLS/DTLS, multi-currency.


% ---------------------------------------------------------
\section{Conclusion}
% ~0.5 pages
% Crisp summary of what was built and key takeaways about distributed systems design.

\end{document}

\end{document}